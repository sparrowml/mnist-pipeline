name: mnist-sweep
method: bayes
metric:
  name: dev_accuracy
  goal: maximize

parameters:
  max_epochs:
    values: [1, 2, 3]
  learning_rate:
    min: 0.0001
    max: 0.1
  batch_size:
    values: [64, 128, 256]

command:
  - mnist
  - train-model
  - ${args}
